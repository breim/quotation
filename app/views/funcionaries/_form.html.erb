<%= form_for(@funcionary) do |f| %>
  <% if @funcionary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funcionary.errors.count, "error") %> prohibited this funcionary from being saved:</h2>

      <ul>
      <% @funcionary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company_id %><br>
    <%= f.number_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
